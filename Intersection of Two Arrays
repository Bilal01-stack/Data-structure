/**
 * @param {number[]} nums1
 * @param {number[]} nums2
 * @return {number[]}
 */
var intersection = function(nums1, nums2) {
    const uniquue = new Set(nums1);
    return [...uniquue].filter((item)=>nums2.indexOf (item)!== -1);
};
