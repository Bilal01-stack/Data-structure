/**
 * @param {number} x
 * @return {boolean}
 */
var isPalindrome = function(x) {
    num = x;
    sum = 0;
    while(x>0){
        sum =  (sum*10) + x%10;
        x= Math.floor(x/10);
    }
    if(num===sum){
        return true
    }else{
        return false
    }
};
